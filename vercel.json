{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install",
  "buildCommand": "npm run shared:build && cd mobile && npm install && EXPO_PUBLIC_BASE_URL='' EXPO_PUBLIC_DEMO=1 npx expo export -p web && cp dist/index.html dist/404.html",
  "outputDirectory": "mobile/dist",
  "cleanUrls": true,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
